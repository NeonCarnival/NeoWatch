name: Minimal Test
on:
  push:
    branches: [ main ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Runner Check
        run: echo "GitHub Runner ist online und funktionsfähig!"
